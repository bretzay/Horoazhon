<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Horoazhon.Features.Medecins.Views.ConsultationListControl"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Horoazhon.Features.Medecins.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Horoazhon.Domain.Models"
    mc:Ignorable="d">

    <Grid>
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">

            <ListView  MinHeight="20" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"   ItemsSource="{x:Bind ItemsSource, Mode=OneWay}" SelectedItem="{x:Bind SelectedItem, Mode=TwoWay}">

                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:Consultation">
                        <TextBlock TextWrapping="WrapWholeWords" MaxWidth="500">
             <Run Text="{Binding Datedebutrdv}" />
             <Run Text=" -> " />
             <Run Text="{Binding Datefinrdv}" />
             <Run Text=" : " />
             <Run Text="{Binding Rendezvou.IdpersClientNavigation.IdpersNavigation.Nompers, TargetNullValue='(Client ?)'}" />
             <Run Text=" : " />
             <Run Text="{Binding Motifcons}" />
                        </TextBlock>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </ScrollViewer>
    </Grid>
</Page>
