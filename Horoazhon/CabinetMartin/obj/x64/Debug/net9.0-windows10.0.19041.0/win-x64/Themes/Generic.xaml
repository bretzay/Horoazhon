<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CabinetMartin">

   <!-- <Style 
    
    
    
    
    
    
    
    Type="local2:PatientEditControl" xmlns:local2="using:CabinetMartin.Features.Rdvs.Views">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local2:PatientEditControl">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="local2:RdvListControl" xmlns:local2="using:CabinetMartin.Features.Rdvs.Views">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local2:RdvListControl">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="local2:ConsultationListControl" xmlns:local2="using:CabinetMartin.Features.Consultation.Views">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local2:ConsultationListControl">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->
</ResourceDictionary>

