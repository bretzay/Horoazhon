<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Horoazhon.Features.Agenda.Views.AgendaPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Horoazhon.Features.Agenda.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Shell="using:Horoazhon.Features.Shell.Views" 
    xmlns:local2="using:Horoazhon.Features.Consultations.Views" 
    xmlns:local3="using:Horoazhon.Features.Clients.Views"
    xmlns:vm="using:Horoazhon.Features.Agenda.ViewModel"
    mc:Ignorable="d">

    <Grid Margin="0,10,0,0">
        <StackPanel Orientation="Horizontal">
            <StackPanel Margin="1">
                <TextBlock Margin="10">Sélection un médécin :</TextBlock>
                <ComboBox ItemsSource="{Binding Medecins, Mode=TwoWay}" SelectedItem="{Binding MedecinSelected, Mode=TwoWay}" Margin="10" >
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock TextAlignment="Center" Margin="1" Padding="0" VerticalAlignment="Center">
                            <Run Text="{Binding IdpersNavigation.Nompers,  Mode=TwoWay}" />
                            </TextBlock>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
            </StackPanel>
            <StackPanel >
                <StackPanel Margin="0,10,0,10">
                    <Grid Width="Auto">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0"  Command="{Binding GoCommand}">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <SymbolIcon Symbol="DockLeft"/>
                                <TextBlock Text="Reculer"/>
                            </StackPanel>
                        </Button>

                        <Button Grid.Column="1" Command="{Binding BackCommand}"   HorizontalAlignment="Right">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <SymbolIcon Symbol="DockRight"/>
                            <TextBlock Text="Avancer"/>
                        </StackPanel>
                        </Button>

                    </Grid>
                </StackPanel>
                <Frame  Background="White" >
                    <Grid>
                        <ScrollViewer MaxHeight="700"  VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Visible" VerticalScrollMode="Enabled" HorizontalScrollMode="Disabled" >
                            <StackPanel Orientation="Horizontal" >
                                <StackPanel>
                                    <TextBlock>
                                        Créneaux
                                        <LineBreak/>
                                        Horaires
                                    </TextBlock>
                                    <ListBox x:Name="SlotName" ItemsSource="{Binding SlotLabelList, Mode=TwoWay}" >
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Height="30" Text="{Binding}" TextAlignment="Center" Margin="0" Padding="0,5,0,0" VerticalAlignment="Bottom" />
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                </StackPanel>
                                <!-- Lundi -->
                                <StackPanel >
                                    <TextBlock Text="{Binding LundiLabel, Mode=TwoWay}" TextAlignment="Center" FontSize="14" FontStretch="Condensed" />
                                    <ListBox ItemsSource="{Binding LundiList, Mode=TwoWay}" Margin="0" Padding="0">
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <Button  Content="{Binding Statut, Mode=TwoWay}" Height="30" d:DataContext="{Binding SlotSelected}"  Margin="0" Padding="0" VerticalAlignment="Center" MinWidth="150"  Click="MoreInfoClick" IsEnabled="{Binding IsEditable}" Background="{Binding BackgroundColor}"/>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                </StackPanel>
                                <!-- Mardi -->
                                <StackPanel >
                                    <TextBlock Text="{Binding MardiLabel}" TextAlignment="Center" FontSize="14" FontStretch="Condensed">
                                    </TextBlock>
                                    <ListBox ItemsSource="{Binding MardiList, Mode=TwoWay}" >
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <Button  Content="{Binding Statut, Mode=TwoWay}" Height="30" d:DataContext="{Binding SlotSelected}" Margin="0" Padding="0" VerticalAlignment="Center" MinWidth="150" Click="MoreInfoClick" IsEnabled="{Binding IsEditable}" Background="{Binding BackgroundColor}"/>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                </StackPanel>
                                <!-- Mercredi -->
                                <StackPanel>
                                    <TextBlock Text="{Binding MercrediLabel}" TextAlignment="Center" FontSize="14" FontStretch="Condensed">
                                    </TextBlock>
                                    <ListBox ItemsSource="{Binding MercrediList, Mode=TwoWay}"  >
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <Button  Content="{Binding Statut, Mode=TwoWay}" Height="30" d:DataContext="{Binding SlotSelected}" Margin="0" Padding="0" VerticalAlignment="Center" MinWidth="150" Click="MoreInfoClick" IsEnabled="{Binding IsEditable}" Background="{Binding BackgroundColor, Mode=TwoWay}"/>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                </StackPanel>
                                <!-- Jeudi -->
                                <StackPanel>
                                    <TextBlock Text="{Binding JeudiLabel, Mode=TwoWay}" TextAlignment="Center" FontSize="14" FontStretch="Condensed">
                                    </TextBlock>
                                    <ListBox ItemsSource="{Binding JeudiList, Mode=TwoWay}" >
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <Button  Content="{Binding Statut, Mode=TwoWay}" Height="30" d:DataContext="{Binding SlotSelected}" Margin="0" Padding="0" VerticalAlignment="Center" MinWidth="150" Click="MoreInfoClick" IsEnabled="{Binding IsEditable}" Background="{Binding BackgroundColor, Mode=TwoWay}"/>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                </StackPanel>
                                <!-- Vendredi -->
                                <StackPanel>
                                    <TextBlock Text="{Binding VendrediLabel, Mode=TwoWay}" TextAlignment="Center" FontSize="14" FontStretch="Condensed">
                                    </TextBlock>
                                    <ListBox ItemsSource="{Binding VendrediList, Mode=TwoWay}" >
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <Button  Content="{Binding Statut, Mode=TwoWay}" Height="30" d:DataContext="{Binding SlotSelected}" Margin="0" Padding="0" VerticalAlignment="Center" MinWidth="150" Click="MoreInfoClick" IsEnabled="{Binding IsEditable}" Background="{Binding BackgroundColor}"/>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                </StackPanel>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </Frame>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
