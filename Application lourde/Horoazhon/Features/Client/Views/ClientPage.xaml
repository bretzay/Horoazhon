<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Horoazhon.Features.Clients.Views.ClientPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Horoazhon.Features.Clients.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:Shell="using:Horoazhon.Features.Shell.Views" 
    xmlns:local1="using:Horoazhon.Features.Personnes.Views" 
    xmlns:local2="using:Horoazhon.Features.Visite.Views"
    mc:Ignorable="d">


    <Grid Padding="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="1.5*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>




        <!-- Liste -->

        <StackPanel Grid.Row="0" Grid.Column="0" Grid.RowSpan="2" >
            <local:ClientsearchControl Margin="0,0,0,10" />

            <local:ClientListControl />
        </StackPanel>

        <!-- Edition des Clients
                <local:ClientEditControl Grid.Column="1" DataContext="{Binding 
Clientselected, Mode=TwoWay}"/>

                <StackPanel Grid.Column="2">
                 
                  -->






        <!-- détails de la Personne -->
        <StackPanel Grid.Row="1" Grid.Column="1" Margin="20,0,0,0" DataContext="{Binding PersonneSelected, Mode=TwoWay}">
            <TextBlock Text="Détails de la Personne" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
            <TextBox Name="Id" Header="Id" PlaceholderText="ID" Text="{Binding Id, Mode=TwoWay}"/>
            <TextBox Header="SIRET" PlaceholderText="SIRET" Text="{Binding Siret, Mode=TwoWay}"/>
            <TextBox Header="Nom" PlaceholderText="Nom" Text="{Binding Nom, Mode=TwoWay}"/>
            <TextBox Header="Prénom" PlaceholderText="Prénom" Text="{Binding Prenom, Mode=TwoWay}" />
            <TextBox Header="Adresse" PlaceholderText="Adresse" Text="{Binding Adresse, Mode=TwoWay}"/>
            <TextBox Header="RIB" PlaceholderText="RIB" Text="{Binding Rib, Mode=TwoWay}" />
            <DatePicker Header="Date de naissance" Date="{Binding Datenais, Mode=TwoWay}" />
        </StackPanel>

        <!-- boutons -->
        <StackPanel Grid.Row="1" Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Top">
            <Button Content="Nouveau" Margin="0,0,0,5" Command="{Binding CommandPersonneNew}" />
            <Button Content="Éditer" Margin="0,0,0,5" Command="{Binding CommandPersonneEdit}" />
            <Button Content="Enregistrer" Margin="0,0,0,5" Command="{Binding CommandPersonnesave}" />
            <Button Content="Supprimer" Margin="0,0,0,5" Command="{Binding CommandPersonneDelete}" />
            <Button Content="Annuler" Margin="0,0,0,5" Command="{Binding CommandPersonneCancel}" />
        </StackPanel>

        <!-- 📅 Rendez-vous 
        <StackPanel Grid.Row="2" Grid.Column="0" Margin="20,10,0,0">
            <TextBlock Text="Rendez-vous" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
            <ListView>
                <local:RdvListControl/>
                <ListViewItem Content="09/09/25 - 09:00 à 09:30 : Dr. House, RV standard" />
                <ListViewItem Content="10/09/25 - 09:00 à 09:30 : Dr. House, test encore" />
                <ListViewItem Content="11/09/25 - 09:00 à 09:30 : Dr. House, test encore" />
                <ListViewItem Content="12/09/25 - 09:00 à 09:30 : Dr. House, test encore" />
                <ListViewItem Content="13/09/25 - 09:00 à 09:30 : Dr. House, test encore" />
            </ListView>
        </StackPanel>

         🩺 Visite 
        <StackPanel Grid.Row="2" Grid.Column="1" Margin="20,10,0,0">
            <TextBlock Text="Visite" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
            <ListView>
            <local:VisiteListControl/>
                <ListViewItem Content="09/09/25 - 09:00 : Consultation générale : Sommeil ok" />
                <ListViewItem Content="10/09/25 - 09:00 : test encore" />
                <ListViewItem Content="11/09/25 - 09:00 : test encore" />
                <ListViewItem Content="12/09/25 - 09:00 : test encore" />
                <ListViewItem Content="13/09/25 - 09:00 : test encore : Hypocondriaque?" />
            </ListView>
        </StackPanel>-->



        <Grid Grid.Row="2" Grid.ColumnSpan="3" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <!-- Neutralized - linked to deleted Agenda service -->
            <!--<Expander Header="Rendes Vous" Grid.Column="0" IsExpanded="True" Margin="10,0,10,0">
                <StackPanel Orientation="Horizontal">
                    <local:RdvListControl DataContext="{Binding RDVs}" />

                </StackPanel>
            </Expander>-->

            <!-- Neutralized - linked to deleted Agenda service -->
            <!--<Expander Header="Visite"   IsExpanded="True"  Grid.Column="1">
                <Border Padding="8"  BorderThickness="1" CornerRadius="6" Background="White">

                    <local:VisiteListControl HorizontalAlignment="Stretch"  VerticalAlignment="Stretch" ItemsSource="{Binding Visite}" SelectedItem="{Binding Visiteelected, Mode=TwoWay}" />
                </Border>
            </Expander>-->

        </Grid>

    </Grid>
</Page>       
        
        
        
        
   
