<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="Horoazhon.Features.Medecins.Views.RdvListControl" 
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
      xmlns:local="using:Horoazhon.Features.Medecins.Views" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      mc:Ignorable="d">
    <Grid>
        <ListView MinHeight="200" VerticalAlignment="Stretch" ItemsSource="{Binding}" SelectedItem="{Binding RdvSelected, Mode=TwoWay}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Padding="8" Spacing="2">
                        <TextBlock FontWeight="SemiBold">
                         <Run Text="{Binding Datedebutrdv}" />
                         <Run Text=" → " />
                         <Run Text="{Binding Datefinrdv}" />
                        </TextBlock>

                        <TextBlock>
                         <Run Text="{Binding IdpersClientNavigation.IdpersNavigation.Nompers}" />
                         <Run Text=" — Dr. " />
                         <Run Text="{Binding IdpersmedecinNavigation.IdpersNavigation.Nompers}" />
                        </TextBlock>

                        <TextBlock Text="{Binding Commentairerdv}" TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>


