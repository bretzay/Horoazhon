<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Horoazhon.Features.Consultations.Views.ConsultationListControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Horoazhon.Features.Consultations.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <ListView MinHeight="50" ItemsSource="{Binding}" SelectedItem="{Binding ConsultationSelected, Mode=TwoWay}" >
            <ListView.ItemTemplate>
                <DataTemplate  >
                    <TextBlock>
                    <Run Text="{Binding Datedebutrdv, Converter={StaticResource DateFormatConverter}, ConverterParameter=dd/MM/yy - HH:mm}"/>
                    <Run Text=" -> "/>
                    <Run Text="{Binding Datefinrdv, Converter={StaticResource DateFormatConverter}, ConverterParameter=dd/MM/yy - HH:mm}"/>
                    <Run Text=" : "/>
                    <Run Text="{Binding Rendezvou.IdpersClientNavigation.IdpersNavigation.Nompers, TargetNullValue='(Client  ?)'}"/>
                    <Run Text=" : "/>
                    <Run Text="{Binding Motifcons, TargetNullValue=''}"/>
                    <Run Text=" : "/>
                    <Run Text="{Binding Notecons, TargetNullValue=''}"/>
                    </TextBlock>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>

