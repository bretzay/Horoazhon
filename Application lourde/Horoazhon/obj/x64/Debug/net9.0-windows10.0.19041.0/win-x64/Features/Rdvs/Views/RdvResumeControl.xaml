<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Horoazhon.Features.Rdvs.Views.RdvResumeControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Horoazhon.Features.Rdvs.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Width" Value="350" />
            <Setter Property="Margin" Value="10" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Width" Value="200" />
            <Setter Property="Margin" Value="10" />
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Width" Value="200" />
            <Setter Property="Margin" Value="10" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <StackPanel>
            <StackPanel>
                <Grid >
                    <StackPanel Margin="5">
                        <TextBlock Text="Sélectionnez un Client pour ouvrir la consultation!" />
                        <TextBlock Text="{Binding RdvDate, Mode=TwoWay}" HorizontalAlignment="Left"/>
                        <TextBlock Text="{Binding RdvNomMedecin, Mode=TwoWay}" HorizontalAlignment="Left"/>
                        <ComboBox ItemsSource="{Binding RdvClients, Mode=TwoWay}" SelectedItem="{Binding RdvClientselected, Mode=TwoWay}" >
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock>
                        <Run Text="{Binding IdpersNavigation.Nompers, Mode=TwoWay}"/>  
                        <Run Text=" "/> 
                        <Run Text="{Binding IdpersNavigation.Prenompers, Mode=TwoWay}"/>
                                    </TextBlock>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>

                        <TextBox Text="{Binding RdvCommentaire, Mode=TwoWay}" HorizontalAlignment="Left" />
                    </StackPanel>


                </Grid>
            </StackPanel>
            <StackPanel Orientation="Horizontal"  >
                <Button Content="Enregistrer" Command="{Binding CommandRdvSave}" />
                <Button Content="Supprimer" Command="{Binding CommandRdvDelete}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>

