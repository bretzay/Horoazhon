<?xml version="1.0" encoding="utf-8"?>
<UserControl x:ConnectionId='1'
    x:Class="Horoazhon.Features.Clients.Views.ConsultationListControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Horoazhon.Features.Clients.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:models="using:Horoazhon.Domain.Models"
    mc:Ignorable="d">

    <Grid>
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">

            <ListView x:ConnectionId='2'     MinHeight="200"
                HorizontalAlignment="Stretch"         
   VerticalAlignment="Stretch"                                                                                                    >
        
                <ListView.ItemTemplate>
                    <DataTemplate                                 >
                        <TextBlock TextWrapping="WrapWholeWords" MaxWidth="500">
                <Run Text="{Binding Datedebutrdv}" />
                <Run Text=" -> " />
                <Run Text="{Binding Datefinrdv}" />
                <Run Text=" : " />
                <Run Text="{Binding Rendezvou.IdpersClientNavigation.IdpersNavigation.Nompers, TargetNullValue='(Client ?)'}" />
                <Run Text=" : " />
                <Run Text="{Binding Motifcons}" />
                        </TextBlock>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </ScrollViewer>
    </Grid>
</UserControl>

       <!-- <UserControl.Resources>
            <conversion:DateFormatConverter x:Key="DateFormatConverter" />
        </UserControl.Resources>-->
        
        
        <!-- … -->

       <!--     <ListView MinHeight="50" ItemsSource="{Binding}" SelectedItem="{Binding ConsultationSelected, Mode=TwoWay}" >
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:Consultation" >
                        <TextBlock>
                            <Run Text="{Binding Datedebutrdv, Converter={StaticResource DateFormatConverter}, ConverterParameter=dd/MM/yy - HH:mm}"/>
                            <Run Text=" -> "/>
                            <Run Text="{Binding Datefinrdv, Converter={StaticResource DateFormatConverter}, ConverterParameter=dd/MM/yy - HH:mm}"/>
                            <Run Text=" : "/>
                            <Run Text="{Binding Rendezvou.IdpersClientNavigation.IdpersNavigation.Nompers, TargetNullValue='(Client  ?)'}"/>
                            <Run Text=" : "/>
                            <Run Text="{Binding Motifcons, TargetNullValue=''}"/>
                            <Run Text=" : "/>
                            <Run Text="{Binding Notecons, TargetNullValue=''}"/>
                    </TextBlock>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>-->

