<?xml version="1.0" encoding="utf-8"?>
<Page x:ConnectionId='1'
    x:Class="Horoazhon.Features.Medecins.Views.VisiteListControl"
    
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Horoazhon.Features.Medecins.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:Horoazhon.Domain.Models"
    mc:Ignorable="d">

    <Grid>
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">

            <ListView x:ConnectionId='2'  MinHeight="20" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"                                                                                                    >

                <ListView.ItemTemplate>
                    <DataTemplate                                 >
                        <TextBlock TextWrapping="WrapWholeWords" MaxWidth="500">
             <Run Text="{Binding Datedebutrdv}" />
             <Run Text=" -> " />
             <Run Text="{Binding Datefinrdv}" />
             <Run Text=" : " />
             <Run Text="{Binding RendezVous.IdpersClientNavigation.IdpersNavigation.Nompers, TargetNullValue='(Client ?)'}" />
             <Run Text=" : " />
             <Run Text="{Binding Motifcons}" />
                        </TextBlock>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </ScrollViewer>
    </Grid>
</Page>

