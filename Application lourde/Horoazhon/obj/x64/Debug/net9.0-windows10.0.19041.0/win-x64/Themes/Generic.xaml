<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Horoazhon">

   <!-- <Style 
    
    
    
    
    
    
    
    Type="local2:ClientEditControl" xmlns:local2="using:Horoazhon.Features.Rdvs.Views">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local2:ClientEditControl">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="local2:RdvListControl" xmlns:local2="using:Horoazhon.Features.Rdvs.Views">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local2:RdvListControl">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="local2:VisiteListControl" xmlns:local2="using:Horoazhon.Features.Consultation.Views">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local2:VisiteListControl">
                    <Border
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}">
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->
</ResourceDictionary>

